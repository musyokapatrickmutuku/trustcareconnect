{
  "name": "@trustcareconnect/frontend",
  "version": "1.0.0",
  "description": "✅ OPERATIONAL: TrustCareConnect HTML Frontend - Integrated with Motoko backend and IC Agent",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 3000",
    "dev": "python -m http.server 3000",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r js dist/ && cp -r css dist/ && [ -d assets ] && cp -r assets dist/ || echo 'No assets directory found'",
    "clean": "rm -rf dist/",
    "rebuild": "npm run clean && npm run build",
    "serve": "cd dist && python -m http.server 8080",
    "test": "echo 'Tests not yet implemented for HTML frontend'",
    "test:watch": "echo 'Tests not yet implemented for HTML frontend'",
    "test:coverage": "echo 'Tests not yet implemented for HTML frontend'",
    "test:ci": "echo 'Tests not yet implemented for HTML frontend'",
    "lint": "echo 'Linting not yet configured for HTML frontend'",
    "lint:fix": "echo 'Linting not yet configured for HTML frontend'",
    "type-check": "echo 'Type checking not applicable for HTML frontend'"
  },
  "dependencies": {},
  "devDependencies": {},
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "keywords": [
    "html",
    "javascript",
    "healthcare",
    "frontend",
    "icp",
    "dfinity",
    "motoko"
  ],
  "author": "TrustCareConnect Team",
  "license": "MIT"
}
