{
  "name": "trustcareconnect",
  "version": "1.0.0",
  "description": "AI-driven diabetes care platform with human oversight - combines AI efficiency with physician safety validation on blockchain infrastructure",
  "scripts": {
    "build": "dfx build",
    "start": "dfx start --background",
    "deploy": "dfx deploy",
    "stop": "dfx stop",
    "clean": "dfx canister --network local delete --all",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "echo 'Backend tests would run here'",
    "test:frontend": "echo 'Frontend tests would run here'"
  },
  "keywords": [
    "icp",
    "motoko",
    "medical-ai",
    "diabetes",
    "healthcare"
  ],
  "devDependencies": {
    "@dfinity/agent": "^0.20.0",
    "@dfinity/candid": "^0.20.0",
    "@dfinity/principal": "^0.20.0"
  }
}